# Internal URL used by Astro SSR to reach the backend (container-to-container)
API_URL=http://vexil-io:6600

# Exposed to the browser — must use a host-accessible address
PUBLIC_API_URL=http://localhost:6600
