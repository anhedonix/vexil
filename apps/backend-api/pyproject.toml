[project]
name = "vexil-backend"
version = "0.1.0"
description = "Backend for Vexil, a tool for managing projects in Houdini"
readme = "README.md"
requires-python = "==3.13.*"
dependencies = [
    "django>=6.0.5",
]
