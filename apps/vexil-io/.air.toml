root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/vexil-io ."
  bin = "./tmp/vexil-io"
  include_ext = ["go", "graphqls"]
  exclude_dir = ["tmp", "node_modules"]
  delay = 1000
  stop_on_error = true

[misc]
  clean_on_exit = true
