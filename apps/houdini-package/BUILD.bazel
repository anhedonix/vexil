load("@rules_python//python:defs.bzl", "py_library")

# Generic Python library for the houdini-package
# Assumes uv is used to manage dependencies via a requirements.txt or pyproject.toml
py_library(
    name = "houdini_package_lib",
    srcs = glob(
        ["**/*.py"],
        exclude = ["tests/**"],
    ),
    visibility = ["//visibility:public"],
    deps = [
        # Dependencies should be referenced from the central requirements repository
        # e.g., "@python_deps//pandas",
    ],
)

# Project has no entry point but will need a cleanup script for publication
# so that the ui files and any developer related files are removed.
