{
  "name": "vexil",
  "description": "Houdini centric project management system",
  "private": true,
  "tasks": {
    "dev": "turbo run dev",
    "setup": "(cd apps/plugins/houdini-package && uv sync) && (cd dev-tools/env-init && uv sync) && (cd apps/backend-api && go mod download)"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.10.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "apps/*",
    "dev-tools/*",
    "apps/plugins/*"
  ]
}
